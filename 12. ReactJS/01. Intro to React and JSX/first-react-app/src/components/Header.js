// export default function Header() {...} also works

function Header() {
    return (
        <header className="App-header">
            <h1>Hello from CRA</h1>
        </header>
    );
}

// Component - Creating function that returns html code

export default Header;

// Exporting the component